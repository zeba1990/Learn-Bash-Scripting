#Given a list of countries, each on a new line, 
#your task is to read them into an array and then display the entire array, 
#with a space between each of the countries' names.

#!/bin/sh
i=0
while read line
do
    array[$i]=$line
    ((i++))
done
echo ${array[@]}
